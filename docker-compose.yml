services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: formula_processor
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/system/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
