[tool.poetry]
name = "formula-processor"
version = "1.0.0"
description = ""
authors = []
packages = [
    {include = "core"},
    {include = "api"},
    {include = "infra"}
]

[tool.poetry.dependencies]
python = "^3.12"
pyodbc = "^5.2.0"
pydantic = "^2.10.5"
pydantic-settings = "^2.7.1"
structlog = "^24.4.0"
fastapi = "^0.115.0"
uvicorn = {extras = ["standard"], version = "^0.32.0"}

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.4"
httpx = "^0.27.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
